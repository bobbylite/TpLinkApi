var React = require('react');
var DefaultLayout = require('./layouts/default');

// Make a UI here with buttons that fetch the REST API.
class tpLinkContainer extends React.Component {
  render() {
    return (
        <div>TP LINK API</div>
    );
  }
}

module.exports = tpLinkContainer;
